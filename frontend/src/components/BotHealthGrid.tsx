import { Bar, BarChart, ResponsiveContainer, Tooltip } from "recharts";
import type { BotMetric } from "@/types/dashboard";
const labels: Record<string, string> = { ddos_bot: "DDoS", beaconing_bot: "Beaconing", dga_dns_bot: "DGA / DNS", encrypted_malware_bot: "Encrypted malware", scanning_bot: "Scanning", exfiltration_bot: "Exfiltration" };
export function BotHealthGrid({ bots, loading = false }: { bots: BotMetric[]; loading?: boolean }) { return <section className="card col-span-full"><div className="card-head"><div><p className="eyebrow">Autonomous defense</p><h2>Specialist bot health</h2></div><span className="text-xs text-slate-500">{bots.filter(b => b.status === "HEALTHY").length}/6 healthy</span></div>{loading ? <p className="py-8 text-center text-sm text-slate-500">Loading bot health…</p> : bots.length === 0 ? <p className="rounded-lg border border-amber-400/30 bg-amber-400/10 p-4 text-sm text-amber-700 dark:text-amber-300">No bot-health records were returned by the backend. The dashboard has no fallback/mock cards.</p> : <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{bots.map(bot => <article className="rounded-xl border border-slate-200 p-3 dark:border-slate-800" key={bot.bot_name}><div className="mb-2 flex justify-between gap-2"><span className="font-medium text-slate-800 dark:text-slate-100">{bot.display_name || labels[bot.bot_name] || bot.bot_name}</span><span className={`status-pill ${bot.status.toLowerCase()}`}>{bot.status}</span></div><div className="h-12"><ResponsiveContainer><BarChart data={[{ value: Math.round((bot.f1_score ?? 0) * 100) }]}><Tooltip cursor={false}/><Bar dataKey="value" fill="#22d3ee" radius={[4, 4, 0, 0]} /></BarChart></ResponsiveContainer></div><div className="mt-2 flex justify-between text-[11px] text-slate-500"><span>Confidence {Math.round((bot.f1_score ?? 0) * 100)}%</span><span>{new Date(bot.last_heartbeat).toLocaleTimeString()}</span></div></article>)}</div>}</section>; }
