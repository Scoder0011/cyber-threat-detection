import { ThemeToggle } from "./ThemeToggle";
export function TopBar({ dark, onToggle, criticals }: { dark: boolean; onToggle: () => void; criticals: number }) {
  return <header className="flex min-h-16 items-center justify-between border-b border-slate-200 bg-white/85 px-5 backdrop-blur dark:border-slate-800 dark:bg-slate-950/85"><div><p className="text-xs font-medium uppercase tracking-[.18em] text-slate-400">Security operations center</p><h1 className="text-lg font-semibold text-slate-900 dark:text-white">Threat intelligence overview</h1></div><div className="flex items-center gap-3"><div className="hidden rounded-lg border border-emerald-500/25 bg-emerald-500/10 px-3 py-2 text-xs text-emerald-700 dark:text-emerald-300 sm:block"><b>92</b>/100 Security score <span className="ml-2">● Protected</span></div>{criticals > 0 && <span className="rounded-full bg-red-500 px-2 py-1 text-xs font-bold text-white">{criticals}</span>}<ThemeToggle dark={dark} onToggle={onToggle}/><div className="grid h-9 w-9 place-items-center rounded-full bg-gradient-to-br from-violet-500 to-cyan-400 text-xs font-bold text-white">SO</div></div></header>;
}
